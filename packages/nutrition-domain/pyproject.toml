[project]
name = "nutrition-domain"
version = "0.1.0"
description = "Episteck Home canonical Nutrition domain (pure Python, deterministic)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
