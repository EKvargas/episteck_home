[project]
name = "episteck_home"
version = "0.1.0"
description = "Privacy-first household coordination core for Episteck Home"
readme = "README.md"
requires-python = ">=3.11"
authors = [{name = "Episteck"}]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
